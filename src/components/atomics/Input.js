import dc from "diet-cola";

const Input = dc("input")(`
    font-family: inherit;
    font-size: inherit;
    padding: 8px;
    margin: 0;
    border-radius: 4px;
    appearance: none;
`);

export default Input;
